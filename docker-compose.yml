services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: USER
      POSTGRES_PASSWORD: PASSWORD
      POSTGRES_DB: contamination_cc
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
  mosquitto:
    image: eclipse-mosquitto:2
    ports: ["1883:1883"]
    volumes: ["./mosquitto.conf:/mosquitto/config/mosquitto.conf"]
volumes:
  pgdata:
