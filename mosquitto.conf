# Local hackathon-demo config only: anonymous connections, no TLS. Not production-appropriate —
# a real deployment needs auth (username/password or client certs) and TLS on the listener.
listener 1883
allow_anonymous true
persistence false
